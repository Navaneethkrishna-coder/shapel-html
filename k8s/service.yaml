apiVersion: v1
kind: Service

metadata:
  name: static-website-service
  namespace: default

spec:
  type: ClusterIP

  selector:
    app: static-website

  ports:
  - port: 80
    targetPort: 80
    protocol: TCP